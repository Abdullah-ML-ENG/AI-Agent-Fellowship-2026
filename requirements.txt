# AI Workspace - Requirements
# This project is a static frontend app (HTML/CSS/JavaScript) with CDN-based dependencies.

# Runtime
Modern web browser (Chrome, Edge, Firefox) with JavaScript enabled

# External APIs
Groq API key
Google Gemini API key

# CDN Dependencies (loaded in index.html)
marked@12.0.2
dompurify@3.1.5

# Optional for local development
Python 3.x (for quick static server)
# Command example:
# python -m http.server 5500
